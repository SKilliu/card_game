package models

func GenerateDeck() *Deck {
	deck := &Deck{}

	var (
		initialDeck Deck
	)
	deckCounter := 0

	for _, sym := range cardsAscending {
		for i := 0; i <= 4; i++ {

		}
	}

	return deck
}
